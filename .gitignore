# environment
.env
.env.local
.env.*.local
*.key
*.pem

# build
dist/
build/
*.js.map

# deps
node_modules/

# logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# test artifacts
coverage/

# local work state (not for git)
WORK.md
implementation_notes/
*.local

# claude local env (safe to commit: .claude/.env.example)
.claude/

# Firestore emulator persistent data
.firebase-data/

# Firebase emulator export snapshots
firebase-export-*/

# Local planning and implementation docs
planning-docs/

# Local dev store (contains API tokens — never commit)
.dev-store.json
.dev-tokens.json

# Terraform local state and secrets (commit only main.tf, variables.tf, *.example, .terraform.lock.hcl)
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.*
terraform/terraform.tfvars
terraform/*.auto.tfvars
terraform/crash.log
terraform/crash.*.log
